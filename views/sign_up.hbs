<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>{{sign_up}}</title>
    {{>headinit}}
  </head>
  <body class="background">
  {{>nav_bar}}
    <div class="wrapper">
      <div id="sign_in">
        <h1>Sign up</h1>
        {{#if error}}
          {{#each error }}
            <p class="alert alert-danger">{{this}}</p>
          {{/each}}
        {{/if}}
        {{#if message}}
          <p class="bg-success">{{message}}</p>
        {{/if}}
        <form action="/insert" method="post">
          <label>E-mail:</label><br> <input id="email" name="email" minlength="5" maxlength="30" type="text" size="30"><br>
          <label>Password:</label><br> <input id="pwd" name="pwd" minlength="5" maxlength="15" type="password" size="20"><br>
          <label>Enter Password Again:</label><br> <input id="pwd2" minlength="5" maxlength="15" name="pwd2" type="password" size="20"><br>
          <br>
          <input type="submit" value="create account">
        </form>
        <br>
        {{message}}

      </div>
      {{>footer}}
      {{>footinit}}
    </div>
  </body>
</html>
